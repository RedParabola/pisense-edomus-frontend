/**
 * Model of the board.
 */
export interface BoardModel {

  /**
   * Id of the board.
   */
  id: string;

  /**
   * Model of the board.
   */
  model: string;

  /**
   * Serial number of the board.
   */
  serialNumber: string;
  
}